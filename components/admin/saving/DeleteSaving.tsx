import { savingI } from "../../../types";
//{`data:${d.photo["Content-Type"]};base64, ${d.photo["data"]}`}

const DeleteSaving = ():JSX.Element => {
    return (
        <div>

        </div>
    )
}

export default DeleteSaving;